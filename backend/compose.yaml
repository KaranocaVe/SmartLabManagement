services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=smart_lab_db'
      - 'MYSQL_PASSWORD=password'
      - 'MYSQL_ROOT_PASSWORD=password'
      - 'MYSQL_USER=root'
    ports:
      - '3306'
